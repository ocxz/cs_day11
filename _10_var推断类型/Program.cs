using System;

namespace _10_var推断类型
{
    class Program
    {
        static void Main(string[] args)
        {
            /**
             *    var 
             *    c# 是强类型语言  代码中，必须对每个变量有个明确的类型定义
             *    js是弱类型
             */

            #region var 的基本使用 利用GetType()获得数据类型
            //var num = 15;
            //var name = "张三";
            //var gender = '男';
            //var salary = 1500m;
            //bool IsGood = true;
            //var height = 1.87;
            //Console.WriteLine("num的类型是{0}\nname的类型是{1}\ngender的类型是{2}\nsalary的类型是{3}\nIsGood的类型是{4}\nheight的类型是{5}", num.GetType(), name.GetType(), gender.GetType(), salary.GetType(), IsGood.GetType(), height.GetType());
            //Console.ReadKey(); 
            #endregion

            #region 使用var变量，在声明变量时必须赋初值

            #endregion
        }
    }
}
